{
  "name": "mocha-gherkin",
  "version": "0.1.11",
  "description": "mocha-gherkin is a Gherkin-style interface and reporter add-on for mocha.",
  "keywords": [
    "mocha",
    "gherkin",
    "bdd",
    "cucumber",
    "test",
    "feature",
    "scenario",
    "user story"
  ],
  "author": "James G. Kim <jgkim@jayg.org> (http://jayg.org/)",
  "bugs": "https://github.com/jgkim/mocha-gherkin/issues",
  "repository": "jgkim/mocha-gherkin",
  "license": "MIT",
  "main": "build/index.js",
  "scripts": {
    "prepublish": "npm run compile",
    "compile": "babel --source-maps true --out-dir build src",
    "lint": "eslint .",
    "pretest": "ln -s .. ./node_modules/mocha-gherkin",
    "test": "mocha --opts tests/mocha.opts --recursive tests",
    "posttest": "rm -f ./node_modules/mocha-gherkin && npm run lint",
    "cover": "istanbul cover `npm bin`/_mocha --report json -- --opts tests/mocha.opts --recursive tests",
    "postcover": "remap-istanbul -i ./coverage/coverage-final.json -o coverage/lcov-remapped.info -t lcovonly",
    "precoveralls": "npm run cover",
    "coveralls": "cat ./coverage/lcov-remapped.info | `npm bin`/coveralls",
    "postcoveralls": "rm -rf ./coverage",
    "clean": "rm -rf ./build",
    "distclean": "rm -rf ./build ./coverage ./node_modules"
  },
  "dependencies": {
    "colors": "latest"
  },
  "peerDependencies": {
    "mocha": "^2.3.4"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.1.18",
    "chai": "latest",
    "coveralls": "^2.11.4",
    "eslint": "^1.10.1",
    "eslint-config-tipplr": "latest",
    "istanbul": "^0.4.1",
    "mocha": "latest",
    "remap-istanbul": "^0.4.0"
  }
}
